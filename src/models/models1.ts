export interface SheepInterface {
    name: string;
    color: string;
}

export enum Color {
    Red = 'red',
    Blue = 'blue',
    Yellow = 'yellow',
    Green = 'green',
    Orange = 'orange',
    White = 'white',
    Black = 'black',
}