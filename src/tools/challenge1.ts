import { SheepInterface } from "../models/models1";

const filterSheeps = (sheeps: SheepInterface[]): SheepInterface[] => 
{
    // TODO: YOUR SHIT
    const newSheeps= [...sheeps];
    return newSheeps;
};

export default filterSheeps;